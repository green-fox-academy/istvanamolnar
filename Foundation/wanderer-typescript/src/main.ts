'use strict';

/*
while (currentLevel < maxLevel) {

  Level1:
  drawTable  √  - but not random
  create and place Hero  √  - class created
  create Enemies  √  - solved with recursion;
  placeCharacters  √  - solved with recursion;

  currentLevel:
    while (gotTheKey is false and still alive) {
      move
      battle
      got the key? => hero.newMap (restores some HP) && currentLevel++ && break;
    }

  Level2
  drawTable;
  keep Hero's stats;
  create Enemies;
  placeCharacters;
  
}
*/